{
  "name": "unwritten-kayworks",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build.mjs",
    "verify": "npm test && npm run build && node scripts/verify.mjs",
    "dev": "npm run build && npx wrangler dev",
    "db:migrate:local": "npx wrangler d1 migrations apply unwritten --local",
    "db:migrate:remote": "npx wrangler d1 migrations apply unwritten --remote",
    "deploy": "npm run verify && npx wrangler deploy"
  }
}
